import random as rand

chars = "abcdefghijklmnopqrstuxvwyz"
CHARS = chars.upper()
symbols = "[]{}()*;/,._-"
numbers = "0123456789"

allVariables = chars + CHARS + symbols + numbers
length = 16
password = "".join(rand.sample(allVariables, length))
print(password)
